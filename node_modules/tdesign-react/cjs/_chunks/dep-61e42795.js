/**
 * tdesign v1.7.1
 * (c) 2024 tdesign
 * @license MIT
 */

'use strict';

var _baseDifference = require('./dep-51813e64.js');
var _baseFlatten = require('./dep-ef5f8027.js');
var _baseRest = require('./dep-b54caa56.js');

var baseDifference = _baseDifference._baseDifference,
  baseFlatten = _baseFlatten._baseFlatten,
  baseRest = _baseRest._baseRest,
  isArrayLikeObject = _baseRest.isArrayLikeObject_1;

/**
 * Creates an array of `array` values not included in the other given arrays
 * using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * for equality comparisons. The order and references of result values are
 * determined by the first array.
 *
 * **Note:** Unlike `_.pullAll`, this method returns a new array.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Array
 * @param {Array} array The array to inspect.
 * @param {...Array} [values] The values to exclude.
 * @returns {Array} Returns the new array of filtered values.
 * @see _.without, _.xor
 * @example
 *
 * _.difference([2, 1], [2, 3]);
 * // => [1]
 */
var difference = baseRest(function (array, values) {
  return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values, 1, isArrayLikeObject, true)) : [];
});
var difference_1 = difference;

exports.difference_1 = difference_1;
//# sourceMappingURL=dep-61e42795.js.map
