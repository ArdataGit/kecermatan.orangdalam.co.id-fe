import { CSSProperties } from 'react';
import TreeNode from '../../_common/js/tree-v1/tree-node';
import { TScroll } from '../../common';
export default function useTreeVirtualScroll({ treeRef, scroll, data, }: {
    data: TreeNode[];
    scroll: TScroll;
    treeRef: React.MutableRefObject<HTMLElement>;
}): {
    scrollHeight: number;
    translateY: number;
    visibleData: any[];
    handleRowMounted: (rowData: any) => void;
    isVirtual: boolean;
    cursorStyle: CSSProperties;
    treeNodeStyle: CSSProperties;
    scrollToElement: (p: import("../../common").ScrollToElementParams) => void;
};
