import { Command } from 'ckeditor5/src/core';
export default class MathCommand extends Command {
    value: string | null;
    execute(equation: string, display?: boolean, outputType?: 'script' | 'span', forceOutputType?: boolean): void;
    display: boolean;
    refresh(): void;
}
