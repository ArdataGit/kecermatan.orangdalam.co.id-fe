import { ButtonView, LabelView, LabeledFieldView, SwitchButtonView, View, type InputTextView } from 'ckeditor5/src/ui';
import { Locale, FocusTracker, KeystrokeHandler } from 'ckeditor5/src/utils';
import MathView from './mathview';
import '../../theme/mathform.css';
import type { KatexOptions } from '../typings-external';
declare class MathInputView extends LabeledFieldView<InputTextView> {
    value: null | string;
    isReadOnly: boolean;
    constructor(locale: Locale);
}
export default class MainFormView extends View {
    saveButtonView: ButtonView;
    mathInputView: MathInputView;
    displayButtonView: SwitchButtonView;
    cancelButtonView: ButtonView;
    previewEnabled: boolean;
    previewLabel?: LabelView;
    mathView?: MathView;
    locale: Locale;
    lazyLoad: undefined | (() => Promise<void>);
    constructor(locale: Locale, engine: 'mathjax' | 'katex' | ((equation: string, element: HTMLElement, display: boolean) => void), lazyLoad: undefined | (() => Promise<void>), previewEnabled: boolean | undefined, previewUid: string, previewClassName: Array<string>, popupClassName: Array<string>, katexRenderOptions: KatexOptions);
    render(): void;
    focus(): void;
    get equation(): string;
    set equation(equation: string);
    focusTracker: FocusTracker;
    keystrokes: KeystrokeHandler;
    private _focusables;
    private _focusCycler;
    private _createMathInput;
    private _createButton;
    private _createDisplayButton;
}
export {};
