import { View } from 'ckeditor5/src/ui';
import type { KatexOptions } from '../typings-external';
import type { Locale } from 'ckeditor5/src/utils';
export default class MathView extends View {
    value: string;
    display: boolean;
    previewUid: string;
    previewClassName: Array<string>;
    katexRenderOptions: KatexOptions;
    engine: 'mathjax' | 'katex' | ((equation: string, element: HTMLElement, display: boolean) => void);
    lazyLoad: undefined | (() => Promise<void>);
    constructor(engine: 'mathjax' | 'katex' | ((equation: string, element: HTMLElement, display: boolean) => void), lazyLoad: undefined | (() => Promise<void>), locale: Locale, previewUid: string, previewClassName: Array<string>, katexRenderOptions: KatexOptions);
    updateMath(): void;
    render(): void;
}
